# LifeGraph 安全设计（更新至 v0.0.10）

## 密钥分层

```text
PIN / 恢复密钥
      ↓ Argon2id
包装密钥
      ↓ AES-GCM
随机仓库主密钥
      ↓ AES-GCM
个人档案、事件、记忆与计划正文
```

## 已落实

- PIN 为 6—12 位数字；
- PIN 不直接充当业务数据密钥；
- 主密钥使用安全随机数生成；
- 恢复密钥与 PIN 使用不同盐和 AAD；
- AES-GCM 同时提供加密与完整性验证；
- 会话令牌仅保存在内存与浏览器 `sessionStorage`；
- 本地服务默认只监听 `127.0.0.1`；
- `data/`、数据库和 `.lifevault` 被 Git 忽略；
- 日志不输出 PIN、恢复密钥或主密钥；
- 修改 PIN 只重新包装同一个主密钥，不批量重写业务密文；
- 更换恢复密钥同样只重新包装恢复密钥槽，PIN 槽和业务密文保持不变；
- 恢复密钥轮换要求再次验证当前 PIN；
- 密钥槽摘要只显示 Argon2id 算法名称和更新时间；
- 安全审计只保存动作代码、成功结果和 UTC 时间戳，最多保留 50 条；
- `.lifevault` 导出前验证 SQLite、外键和全部加密记录；
- 导入先在临时目录完成恢复演练，正式替换前生成安全回滚包；
- 自动备份只保存加密仓库与完整性清单，不保存解密后的姓名或内容；
- 新自动备份在记录成功前重新读取落盘文件，并验证结构、校验和、SQLite、外键和全部密文；
- 最近备份快速验证只读取备份文件，不替换或修改当前仓库；
- 自动备份下载、删除和清空都要求有效解锁会话；
- 历史文件名经过目录边界校验，不能借助路径穿越访问其他文件。


## v0.0.10 资料时间与自动扫描安全边界

为支持数十万资料的年/月/日范围查询，v0.0.10 将“规范化后的时间事实”和“私人原始元数据”分层处理：

- `timeline_at / timeline_end_at / time_precision / time_source / time_confidence / timezone_offset` 为结构化查询字段，可在 SQLite 中被索引；
- 原始 EXIF 字段、文档 metadata、文件名等继续保存在 AES-GCM 加密 metadata 中；
- 手工修正资料时间时，旧识别时间保存在加密 `original_time` 中；
- 自动扫描源的绝对路径以仓库主密钥加密保存；
- 增量扫描索引只保存相对路径哈希、文件大小、mtime、文件身份和状态，不保存可直接浏览的完整目录树；
- 自动扫描发现源文件删除时，只标记源缺失，不自动删除 LifeGraph 已归档的加密副本。

这意味着攻击者若只获得未解锁的 SQLite，仍可能观察到资料时间分布等结构化索引事实，但不能直接读取文件名、正文、原始 EXIF 或扫描源路径。

## 恢复密钥轮换与审计

`vault.json` 中可以保存：

- `key_slot_updated_at.pin`：PIN 密钥槽最近包装时间；
- `key_slot_updated_at.recovery`：恢复密钥槽最近包装时间；
- `security_updated_at`：最近一次安全设置更新时间；
- `security_audit`：最多 50 条安全操作摘要。

审计记录不包含：

- PIN 或恢复密钥；
- 主密钥或包装密钥；
- 姓名、出生日期、标题或正文；
- 恢复密钥长度、片段或哈希。

更换恢复密钥后，原恢复密钥槽被原子替换，因此原凭据不能再解锁当前仓库。此前已经导出的 `.lifevault` 包含导出时的 `vault.json`，仍需使用导出当时的 PIN 或恢复密钥。

## 自动备份策略元数据

`vault.json` 中的 `backup_policy` 可以包含：

- 是否启用；
- 每天或每周周期；
- 保留数量；
- 最近尝试、成功与错误时间；
- 最近备份文件名和非敏感错误摘要；
- 最近完整验证时间、验证文件名和非敏感验证错误摘要。

该策略不包含姓名、出生日期、内容标题、正文、PIN、恢复密钥或主密钥明文。它随 `.lifevault` 一起迁移，使恢复后的仓库可以保留原自动备份偏好。

## 原型限制

- Python 无法保证主密钥内存被立即物理清零；
- 当前使用字段级加密，数据库表结构、规范化资料时间及统计索引仍可见；
- 尚未实现错误 PIN 的持久化限速；
- 尚未实现操作系统安全凭据存储；
- 本地自动备份仍位于同一设备，不能替代异盘或异地备份；
- 尚未经过第三方安全审计。
