# v0.0.1 验收记录

**日期：** 2026-08-05

## 自动化测试

```text
3 passed
```

覆盖：

- 主密钥 PIN 包装与解包；
- 错误凭据拒绝；
- 初始化、档案读取、锁定、错误 PIN、重新解锁；
- 生命进度 API。

## 启动冒烟测试

通过：

- `GET /health` 返回 200；
- `GET /api/v1/system/status` 返回 200；
- 初始化接口返回有效会话；
- 生命进度接口正确处理 2 月 29 日出生日期；
- 锁定后旧会话失效；
- 错误 PIN 返回 401；
- SQLite 文件中未检出测试档案名称明文；
- 前端 JavaScript 通过 `node --check` 语法检查。

## 当前已知限制

- 采用字段级 AES-GCM 加密，尚未切换 SQLCipher 整库页级加密；
- 尚未实现持久化错误 PIN 限速；
- 尚未实现事件、记忆与计划 CRUD；
- 尚未实现 `.lifevault` 导出恢复；
- 尚未实现浏览器插件；
- 尚未执行 Windows 实机界面验收。
