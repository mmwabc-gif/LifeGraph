# Changelog

## v0.0.2.4 - 2026-08-06

- 新增未来计划最小闭环：今天及未来日期可添加、加密保存并按日期读取计划；
- 新增 `POST /api/v1/plans` 接口和计划请求校验；
- 过去日期禁止新增未来计划，并返回 `PLAN_DATE_IN_PAST` 明确错误；
- 计划标题与正文采用独立 AES-GCM AAD 加密，不以明文写入 SQLite、WAL 或 SHM；
- 日期详情接口同时返回事件、记忆、计划及三类内容状态；
- 日期格使用蓝色空心圆环表示“有计划”，可与事件圆点及记忆边框同时显示；
- 悬停放大镜同步显示“有计划”状态；
- 日期详情新增计划表单、计划列表、过去日期提示和独立卡片样式；
- 新增计划范围、加密及三类内容同日共存测试；
- 前后端构建版本提升至 `0.0.2.4`。

## v0.0.2.3 - 2026-08-06

- 新增个人记忆最小闭环：可在日期详情中添加、加密保存并按日期读取记忆；
- 新增 `POST /api/v1/memories` 接口和记忆请求校验；
- 记忆标题与正文采用独立 AES-GCM AAD 加密，不以明文写入 SQLite、WAL 或 SHM；
- 日期详情接口同时返回事件、记忆及对应内容状态；
- 日期格使用柔和边框表示“有记忆”，可与事件圆点同时显示；
- 悬停放大镜同步显示“有事件”和“有记忆”状态；
- 日期详情新增记忆表单、记忆列表和独立卡片样式；
- 新增记忆范围、加密及事件/记忆同日共存测试；
- 前后端构建版本提升至 `0.0.2.3`。

## v0.0.2.2 - 2026-08-05

- 新增生命日期格悬停放大镜，约 8 倍显示鼠标附近的局部日期格；
- 当前日期格使用白色外框和深色内框双重锁定，提升微小格子的定位准确性；
- 放大镜同步显示完整日期、年龄、本生命年第几天、时间状态和事件状态；
- 浮层自动避让窗口边缘，且不拦截鼠标点击；
- Canvas 滚动、离开、窗口缩放或打开日期详情时自动隐藏放大镜；
- 触屏设备保持原有点击交互，不显示悬停浮层；
- 前后端构建版本提升至 `0.0.2.2`。

## v0.0.2.1 - 2026-08-05

### Added

- 新增 schema v1 → v2 非破坏性自动迁移；
- 新增 `events`、`memories`、`plans` 三张加密内容表及日期索引；
- 新增正式事件 AES-GCM 加密写入与按日期解密读取；
- 新增 `GET /api/v1/dates/{date}` 日期详情接口；
- 新增 `GET /api/v1/dates/content-status` 稀疏日期状态接口；
- 新增 `POST /api/v1/events` 添加事件接口；
- Canvas 生命日期格支持鼠标点击命中；
- 新增右侧日期详情抽屉和事件添加表单；
- 有事件的日期格显示独立状态圆点；
- 新增数据库迁移、事件闭环、明文不落盘和日期范围测试。

### Changed

- 前后端构建版本提升至 `0.0.2.1`；
- Python 包版本提升至 `0.0.2`；
- 首页说明更新为日期详情与事件最小闭环。

## v0.0.1 - 2026-08-05

### Added

- Stage 0 初始工程骨架；
- FastAPI `/api/v1`；
- 本地初始化、解锁与锁定流程；
- 随机主密钥、PIN 和恢复凭据分层；
- Argon2id + AES-GCM 加密原型；
- 加密个人档案存储；
- 人生、年度和月度进度计算；
- Canvas 100 年生命日期格；
- Windows 启动脚本；
- 基础 API 与加密测试；
- GitHub 首次发布说明。


### Fixed

- 启动脚本优先使用 `python`，可用时再兼容 Windows `py` 启动器；
- 修复未安装 `py` 启动器时无法自动创建虚拟环境的问题。


### Python 3.10 compatibility

- 最低 Python 版本由 3.11 调整为 3.10；
- 启动脚本支持 Python 3.10.11；
- 已按 Python 3.10 语法规则检查现有源码，未发现 3.11 专属语法。

### Fixed

- 修复 Python 3.10 下 `ImportError: cannot import name 'UTC' from 'datetime'`；
- 将 Python 3.11 的 `datetime.UTC` 改为 Python 3.10 兼容的 `timezone.utc`。

### Fixed

- 修复初始化仓库时 `timezone` 参数遮蔽 `datetime.timezone` 导致的 500 错误；
- 修复前端初始化时只提示“响应格式错误”而不显示后端真实错误的问题；
- 新增全局异常 JSON 响应，Stage 0 调试阶段便于定位启动和初始化错误。


### Fixed

- 修复初始化后计算进度时 `ZoneInfoNotFoundError: No time zone found with key timezone.utc`；
- 将误写入的 `timezone.utc` 字符串自动规范化为 `UTC`；
- Windows 环境增加 `tzdata` 依赖，用于支持 `Asia/Tokyo` 等 IANA 时区；
- 无效时区名会安全回退到 UTC，避免接口返回 500。

### Fixed

- 修复首页偶发视觉跳动：页面固定滚动条空间，避免视图切换时左右抖动；
- 生命格 Canvas 增加尺寸签名缓存，尺寸和日期未变化时不重复重绘；
- `resize` 事件增加防抖，并在重绘后恢复画布容器滚动位置；
- 画布滚动区域增加滚动隔离，减少浏览器滚动联动造成的页面跳动。

### Fixed

- 将生命格鼠标提示从页面固定定位改为生命格容器内定位；
- 岁数与日期提示现在显示在对应日期格旁边，不再漂到页面右下角；
- 提示浮层样式调轻，并限制在画布区域内显示。

### Changed

- 首页顶部信息区改为固定显示，页面滚动时不再跟随生命格一起滚走；
- 档案信息、生命进度和视图切换作为控制区固定在顶部；
- 生命格区域增加滚动定位间距，避免跳转或滚动时被顶部固定区遮挡。

### Fixed

- 修复登录成功后偶发 `Cannot read properties of null (reading 'reset')`；
- 顶部标题栏改为真正固定定位，滚动页面时不再离开顶部；
- 首页档案信息、生命进度和统计卡片增加真实 `.home-sticky` 包裹层，确保固定规则能命中；
- 生命格岁数/日期提示改为基于 `canvas-wrap` 可视区域定位，不再漂到页面右下角。

### Fixed — build 0.0.1.7

- 删除解锁成功后的表单 `.reset()` 调用，彻底消除 `Cannot read properties of null (reading 'reset')`；
- CSS/JS 地址加入构建版本号，并由后端对首页与静态资源返回 `Cache-Control: no-store`；
- 页面顶部标题栏使用明确的固定定位；
- 首页档案和生命进度区使用精确的 `#homeView:not(.hidden) .home-sticky` 固定规则；
- 岁数/日期信息改为生命格标题旁的固定标签，不再使用页面悬浮定位；
- 页面顶部显示 `v0.0.1.7`，可直接确认浏览器是否加载了本次修复。

### Fixed — build 0.0.1.8

- 重新按真实 HTML 结构修复页面，不再依赖猜测式 CSS 覆盖；
- 顶部栏改为通栏固定：去掉卡片式圆角、边框和居中浮动样式；
- 登录页增加 `.auth-card`，解锁表单在模块区域内居中显示；
- 登录脚本彻底移除 `.reset()`，并对表单和按钮做空值保护；
- 岁数/日期提示移动到“已经走过的日子”标题旁边，鼠标移入生命格后直接更新该标签；
- 前端版本提升至 `v0.0.1.8 · JS`，可确认新的 JavaScript 已经加载。

### Changed — build 0.0.1.9

- 生命格岁数/日期标签从“已经走过的日子”标题后方移到右侧；
- 右侧区域统一显示岁数/日期标签和图例，保持靠右对齐；
- 小屏幕下自动恢复上下排列，避免挤压标题。

### Fixed — build 0.0.1.10

- 年度进度和本月进度改为按当前时刻精确计算，不再只按日期整数计算；
- 8 月 5 日晚上这类场景下，本月进度会按小时计算，剩余显示为“26 天 1 小时”这类格式；
- 进度接口新增 `now` 和 `remaining_text` 字段，前端优先使用可读剩余时间文本；
- 新增本月进度精确计算测试。
