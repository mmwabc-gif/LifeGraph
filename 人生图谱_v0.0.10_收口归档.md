# 人生图谱 v0.0.10 收口归档

日期：2026-08-10  
正式版本：v0.0.10  
数据库 schema：v11  
`.lifevault`：format v3

## 阶段定位

v0.0.10 完成“自动资料时间索引与十字时间轴”闭环。LifeGraph 不再只依赖用户主动记录，而是能够从已有和持续增长的照片、视频、文档中自动发现时间事实，将资料映射到真实人生时间结构。

核心产品原则固定为：

> 用户负责生活，系统负责收集、识别、整理和串联。

## 本阶段完成

### 1. 结构化时间模型

- schema v8 → v9；
- 附件新增可索引的时间、结束时间、精度、来源、置信度和时区；
- 原始识别时间继续保留在加密 metadata；
- 为后续大规模范围查询建立 `timeline_at` 索引。

### 2. 旧资料时间回填

- 后台显式任务，不阻塞启动；
- 支持暂停、继续、失败隔离与重启续跑；
- 已有加密时间信息只同步镜像字段，不重写附件；
- 真正缺失时间信息时才重新读取源资料识别。

### 3. 时间统计索引

- schema v10；
- 年/月/日/小时轻量统计；
- 首次需要时纯 SQL 汇总，不解密 metadata；
- 后续通过 SQLite 触发器增量维护。

### 4. 十字时间轴

- 年/月/日三条横轴常驻；
- 每个时间段本身为色带轴段；
- `[数量]` 直接表达资料密度；
- 无资料项弱化并禁用点击；
- 默认当前年；
- 点击年/月自动落到最近有资料日期；
- 选中日后展开 00:00→24:00 日内纵轴；
- 文件只显示轻量信息，媒体按需加载。

### 5. 大量资料场景

- 日内资料压缩为紧凑单行；
- 分钟/10分钟/小时自适应聚合；
- 分页继续加载；
- 上一/下一有资料日期；
- 浏览器屏外内容渲染优化。

### 6. 本机自动扫描

- schema v11；
- 扫描源长期配置、解锁后增量扫描、立即扫描、暂停/继续；
- 普通资料复用 blob-v1；
- 大型资料直接从本机磁盘流式进入 chunked-v1；
- 未变化文件跳过；
- 重命名/移动尽量避免重复；
- 源文件删除只标记缺失，不删除归档副本；
- 扫描路径加密保存并随核心备份恢复。

### 7. 人生图谱联动

- 日期格使用结构化资料统计自动点亮；
- 首页不再逐条解密附件判断资料状态；
- “时间待确认”集中处理低置信度资料；
- 手工修正到日期或秒级，并保留原始时间。

### 8. 年/月/日详情抽屉性能

- 删除“先 `list_all_attachments()` 全库解密再筛选”的旧路径；
- 先使用 `timeline_at` 数据库索引筛当前范围；
- 当前页才解密，默认 12 项/批；
- 继续加载按钮放在文件列表底部，并保留滚动自动加载。

### 9. 首页与资料中心 UI 收口

- 应用启动默认进入首页；
- 首页三项大进度卡压缩为顶部主模块底部信息带；
- 资料中心明确拆分“时间轴 / 列表”模式；
- 自动扫描作为二级顶层弹窗，修复层级遮挡。

## 最终技术状态

```text
应用版本：0.0.10
数据库：schema v11
.lifevault：format v3
普通附件：blob-v1
大型媒体：chunked-v1
时间统计：年 / 月 / 日 / 小时增量索引
自动扫描：本机授权目录增量扫描
右侧抽屉资料：12 项/批索引分页
```

## 验证

```text
264 passed (37 test files, split verification)
JavaScript syntax check passed
Python compile check passed
```

## 收口结论

v0.0.10 已完成“资料自动进入人生时间结构”的第一条完整主链：**自动扫描 → 时间识别 → 结构化索引 → 十字时间轴 → 人生图谱自动点亮**。

后续地点、人物、AI 辅助整理和远端同步都可以建立在这套时间骨架之上，不应反过来增加日常手工整理负担。
